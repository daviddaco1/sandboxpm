# sandboxpm
Secure Node.js package manager with zero silent script execution. Downloads packages directly from the registry, verifies SHA-512 integrity, stores files once via content-addressable store with hard links, and always asks before running any install script.
